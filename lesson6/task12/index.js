  function cloneArr(arr) {
      let newArray = [...arr];
      return newArray;
  };
  /*
  Клон массива
   Напишите функцию cloneArr(arr), которая будет клонировать входящий массив и возвращать новый массив.
   (1.Объявляем внутри фунциии новую переменную и присваиваем ей деструктурированый исходный массив.
    2.Возвращаем эту переменную.)
  */